name: Deploy to GCP Cloud Run

on:
  push:
    branches: [ main, master ]
    paths:
      - 'Dockerfile'
      - 'docker-compose.yml'
      - 'configs/**'
      - 'scripts/**'
      - '.github/workflows/deploy-gcp-cloudrun.yml'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production
      project_id:
        description: 'GCP Project ID'
        required: false
        type: string
      region:
        description: 'Cloud Run region'
        required: false
        type: string
      service_name:
        description: 'Cloud Run service name'
        required: false
        type: string

env:
  PROJECT_ID: ${{ github.event.inputs.project_id || vars.GCP_PROJECT_ID || secrets.GCP_PROJECT_ID }}
  REGION: ${{ github.event.inputs.region || vars.GCP_REGION || 'europe-west1' }}
  SERVICE_NAME: ${{ github.event.inputs.service_name || vars.CLOUD_RUN_SERVICE_NAME || 'mi-soar-ngfw' }}

jobs:
  test:
    name: Test Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: |
          docker build -t $SERVICE_NAME:test .

      - name: Validate Docker Compose
        run: |
          docker compose config

      - name: Validate configurations
        run: |
          # Check Suricata config
          docker run --rm -v $(pwd)/configs/suricata:/etc/suricata:ro jasonish/suricata:latest -T -c /etc/suricata/suricata.yaml || echo "Suricata config validation failed"

          # Check nftables config
          docker run --rm -v $(pwd)/configs/nftables:/etc/nftables:ro debian:bookworm-slim sh -c "
            apt-get update && apt-get install -y nftables &&
            nft --check -f /etc/nftables/main.nft
          " || echo "nftables config validation failed"

  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: test
    environment: staging
    if: github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'staging')
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate deployment configuration
        run: |
          if [[ -z "$PROJECT_ID" ]]; then
            echo "::error::PROJECT_ID is empty. Set workflow input 'project_id', variable 'GCP_PROJECT_ID', or secret 'GCP_PROJECT_ID'."
            exit 1
          fi
          if [[ -z "$REGION" ]]; then
            echo "::error::REGION is empty. Set workflow input 'region' or variable 'GCP_REGION'."
            exit 1
          fi
          if [[ -z "$SERVICE_NAME" ]]; then
            echo "::error::SERVICE_NAME is empty. Set workflow input 'service_name' or variable 'CLOUD_RUN_SERVICE_NAME'."
            exit 1
          fi

      - name: Authenticate to Google Cloud (WIF)
        if: ${{ secrets.GCP_WIF_PROVIDER != '' && secrets.GCP_SERVICE_ACCOUNT != '' }}
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}
          project_id: ${{ env.PROJECT_ID }}

      - name: Authenticate to Google Cloud (Service Account Key)
        if: ${{ !(secrets.GCP_WIF_PROVIDER != '' && secrets.GCP_SERVICE_ACCOUNT != '') && secrets.GCP_SA_KEY != '' }}
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
          project_id: ${{ env.PROJECT_ID }}

      - name: Fail if GCP credentials are missing
        if: ${{ !((secrets.GCP_WIF_PROVIDER != '' && secrets.GCP_SERVICE_ACCOUNT != '') || secrets.GCP_SA_KEY != '') }}
        run: |
          echo "::error::No GCP auth credentials found. Configure WIF (GCP_WIF_PROVIDER + GCP_SERVICE_ACCOUNT) or GCP_SA_KEY."
          exit 1

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Configure Docker for GCR
        run: |
          gcloud auth configure-docker --quiet

      - name: Build and push Docker image
        run: |
          IMAGE_NAME="gcr.io/$PROJECT_ID/$SERVICE_NAME:staging-${{ github.sha }}"
          docker build -t $IMAGE_NAME .
          docker push $IMAGE_NAME

      - name: Deploy to Cloud Run
        run: |
          IMAGE_NAME="gcr.io/$PROJECT_ID/$SERVICE_NAME:staging-${{ github.sha }}"
          gcloud run deploy $SERVICE_NAME-staging \
            --image $IMAGE_NAME \
            --region $REGION \
            --platform managed \
            --allow-unauthenticated \
            --cpu 2 \
            --memory 4Gi \
            --max-instances 5 \
            --timeout 300 \
            --concurrency 80 \
            --set-env-vars="ENVIRONMENT=staging" \
            --set-env-vars="LOG_LEVEL=info" \
            --update-labels="commit-sha=${{ github.sha }}"

      - name: Run health checks
        run: |
          SERVICE_URL=$(gcloud run services describe $SERVICE_NAME-staging \
            --region $REGION \
            --platform managed \
            --format 'value(status.url)')

          echo "Service URL: $SERVICE_URL"

          # Wait for service to be ready
          sleep 30

          # Basic health check
          curl -f "$SERVICE_URL/healthz" || echo "Health check failed"

  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: test
    environment: production
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'production'
    permissions:
      contents: 'write'
      id-token: 'write'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate deployment configuration
        run: |
          if [[ -z "$PROJECT_ID" ]]; then
            echo "::error::PROJECT_ID is empty. Set workflow input 'project_id', variable 'GCP_PROJECT_ID', or secret 'GCP_PROJECT_ID'."
            exit 1
          fi
          if [[ -z "$REGION" ]]; then
            echo "::error::REGION is empty. Set workflow input 'region' or variable 'GCP_REGION'."
            exit 1
          fi
          if [[ -z "$SERVICE_NAME" ]]; then
            echo "::error::SERVICE_NAME is empty. Set workflow input 'service_name' or variable 'CLOUD_RUN_SERVICE_NAME'."
            exit 1
          fi

      - name: Authenticate to Google Cloud (WIF)
        if: ${{ secrets.GCP_WIF_PROVIDER != '' && secrets.GCP_SERVICE_ACCOUNT != '' }}
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}
          project_id: ${{ env.PROJECT_ID }}

      - name: Authenticate to Google Cloud (Service Account Key)
        if: ${{ !(secrets.GCP_WIF_PROVIDER != '' && secrets.GCP_SERVICE_ACCOUNT != '') && secrets.GCP_SA_KEY != '' }}
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
          project_id: ${{ env.PROJECT_ID }}

      - name: Fail if GCP credentials are missing
        if: ${{ !((secrets.GCP_WIF_PROVIDER != '' && secrets.GCP_SERVICE_ACCOUNT != '') || secrets.GCP_SA_KEY != '') }}
        run: |
          echo "::error::No GCP auth credentials found. Configure WIF (GCP_WIF_PROVIDER + GCP_SERVICE_ACCOUNT) or GCP_SA_KEY."
          exit 1

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Configure Docker for GCR
        run: |
          gcloud auth configure-docker --quiet

      - name: Build and push Docker image
        run: |
          IMAGE_NAME="gcr.io/$PROJECT_ID/$SERVICE_NAME:production-${{ github.sha }}"
          docker build -t $IMAGE_NAME .
          docker push $IMAGE_NAME

      - name: Deploy to Cloud Run
        run: |
          IMAGE_NAME="gcr.io/$PROJECT_ID/$SERVICE_NAME:production-${{ github.sha }}"
          gcloud run deploy $SERVICE_NAME-production \
            --image $IMAGE_NAME \
            --region $REGION \
            --platform managed \
            --cpu 4 \
            --memory 8Gi \
            --max-instances 10 \
            --timeout 600 \
            --concurrency 80 \
            --set-env-vars="ENVIRONMENT=production" \
            --set-env-vars="LOG_LEVEL=info" \
            --set-env-vars="SURICATA_IFACE=eth0" \
            --update-labels="commit-sha=${{ github.sha }}" \
            --no-allow-unauthenticated

      - name: Configure IAM
        run: |
          # Grant Cloud Run invoker permission
          gcloud run services add-iam-policy-binding $SERVICE_NAME-production \
            --region $REGION \
            --platform managed \
            --member="serviceAccount:${{ secrets.GCP_SA_EMAIL }}" \
            --role="roles/run.invoker"

      - name: Run comprehensive health checks
        run: |
          SERVICE_URL=$(gcloud run services describe $SERVICE_NAME-production \
            --region $REGION \
            --platform managed \
            --format 'value(status.url)')

          echo "Production Service URL: $SERVICE_URL"

          # Wait for service to stabilize
          sleep 60

          # Run health checks
          for i in {1..10}; do
            if curl -f "$SERVICE_URL/healthz"; then
              echo "Health check passed"
              break
            fi
            echo "Health check attempt $i failed, retrying in 10 seconds..."
            sleep 10
          done

      - name: Create deployment tag
        run: |
          git tag "deploy/production/${{ github.sha }}"
          git push origin "deploy/production/${{ github.sha }}"
