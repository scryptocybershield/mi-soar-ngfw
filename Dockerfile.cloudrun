FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

# Cloud Run requires the container to listen on $PORT (default 8080).
COPY scripts/deploy/cloudrun_server.py /app/cloudrun_server.py

EXPOSE 8080

CMD ["python", "/app/cloudrun_server.py"]
